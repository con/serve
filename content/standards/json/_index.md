---
title: "JSON"
description: "JavaScript Object Notation -- the primary structured metadata format in the vault"
---

[JSON](https://www.json.org/) is the primary format for structured metadata
throughout the con/serve ecosystem -- BIDS sidecar files, tool output,
API responses, and per-item metadata records.
Stored in git (not git-annex) for diffability and searchability.
