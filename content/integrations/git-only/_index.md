---
title: "git-only"
description: "Tool that stores output in git without annex support"
---

The tool stores output in git without annex support.
Suitable for text-based artifacts where git's native tracking is sufficient.
